import React from 'react';
import styled from "styled-components";

const Box = styled.div`
  width: 200px;
  height: 200px;
  background: green;
  transform: translateX(-150px);
  transition: transform 400ms ease-in;
  
`;
const BuildingCount = () => {
    return (
        <Box>

        </Box>
    );
};

export default BuildingCount;